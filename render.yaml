services:
  - type: web
    name: n8n
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: N8N_BASIC_AUTH_ACTIVE
        value: true

      - key: N8N_PROTOCOL
        value: https

      - key: GENERIC_TIMEZONE
        value: Africa/Luanda
